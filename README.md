# Results

This directory is reserved for the paper's reported benchmark results and future reproducibility runs.

Use separate files for:
- `reported_results.csv` — values reported in the manuscript
- `reproduced_results.csv` — values obtained by an actual rerun

Do not label reported values as reproduced unless the original experiment has been rerun.
